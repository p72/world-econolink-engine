# CLAUDE.md

論文「World EconoLink Engine v1」（<https://note.com/ryoya1106/n/ncad5ac10002d>）の日米月次マクロ計量モデルを Python で再現するプロジェクト。
元のコード・Notebook・学習済みモデルは論文に含まれていないので、論文の記述（特に付録A 表7・表8）から作り直している。

- 使い方とデータの出典：`README.md`
- 考え方とコードの解説：`docs/beginner_guide.md`

## ユーザーの方針（守ること）

- **会話もドキュメントも日本語**で書く。コード中のコメントも日本語。
- **データは公式統計だけ**を使う。代理系列や、市場価格（yfinance など）は使わない。論文はドル円・WTI を yfinance から取っているが、ユーザーの判断で公式統計（日銀の月中平均、EIA のスポット価格）にしている。
- 公表されている前年比があれば、自分で計算せずにそれを使う（実質賃金・住宅着工）。
- **出力は PNG**（論文の PDF は使わない。PDF 関連コードは削除済み）。
- 論文との違いや、根拠のない補完をしたときは、隠さず README の「論文からの補完・拡張」「論文との対応の検証」に記録する。

## コマンド

```bash
pip install -r requirements.txt
python run.py train                     # data/us.csv + data/jp.csv で28式を推定 → models/*.pkl
python run.py simulate --scenario scenarios/2026-06_real.json --run-name 2026-06_real
python run.py fetch                     # データ再取得。日本分は環境変数 ESTAT_APP_ID が必要
python run.py demo                      # 合成データでの動作確認
```

- `models/*.pkl` と `outputs/` は git 管理外。クローン直後は `train` が必要（データは commit 済みなのでキー不要）。
- **`demo` は `models/` を合成データの係数で上書きする。** 実データで予測・検証する前に `train` をやり直すこと。過去に、これに気づかず実データのシミュレーションを出してしまったことがある。
- テストスイートは無い。変更後は `train` → `simulate` を実行し、推定結果の表と予測値、GDPギャップ式の係数（下記）で確認する。
- e-Stat の appId をファイルやコミットに書かないこと（公開リポジトリ）。

## 構成の要点

- `econolink/equations.py`：28式を `Equation(key, target, features, mode)` で宣言し、`STEPS` で①〜⑳の順序を定義。説明変数は `g(var, lag)` を受け取る関数で書く。
  - 学習時の `g` は `df[col].shift(lag)`（`estimate.py`）、予測時の `g` は履歴からの参照（`engine.py`）。同じ定義を両方で使うので、学習と予測で説明変数の作り方がずれない。
- `engine.py`：予測時の `g(var, 0)` は、その期にまだ計算していない変数を参照すると `CircularReferenceError` になる。式や順序を変えたら、1期回して確認する。
- 変数名は、コード内では `jp.inflation`、CSV の列名では `jp_inflation`（`variables.col()` で変換）。
- 推定では**説明変数だけを標準化**し、目的変数は元の単位のまま（論文表4の定数項がゼロでないことから判断）。`mode="diff12"` の3式（jp_long_rate_v2, us_unemployment_v1, us_manufacturing_confidence_v1）は前年差を推定して水準に戻す。
- `estimate.py` は `statsmodels.api` を import しない（開発した Windows 環境で、tsa 系の DLL がアプリ制御ポリシーにブロックされたため）。`OLS` などを個別に import している。scikit-learn は使わず `StandardScaler` を自前で実装している。
- シナリオの `add_factor_decay`（アドファクター）と `us_short_rate`（FF金利の上書き）は論文に無い拡張。既定は無効。

## 論文の解釈で決めたこと（変えるなら根拠を持って）

- **出典は2つある。混同しないこと。**
  - **技術論文**（2026-09-09、全22ページ）：World EconoLink Engine v1。日本19式＋米国9式＝28式。**再現対象はこっち**。係数が載っているのは表4（GDPギャップ式）だけ。
  - **前身記事**（2026-05-09、note）：MacroEconoLink Engine v1。日本ブロック14式のみの旧版だが、**本文に係数が多数載っている**（CPI式ほか）。`jp_usd_jpy` `jp_wage` `jp_unemployment` は v1 と v2 で別物なので、共通する式だけ照合できる。
- **検証の基準は技術論文 表4（jp_gdp_gap_v1）**。現状の実データ推定（2007-01〜2025-01、N=217）は、定数 −0.477 / 景気ウォッチャー 0.750 / 輸出数量 0.922 / 米GDPギャップ 0.846 / R² 0.761 / DW 0.916。論文は −0.464 / 0.633 / 0.916 / 0.790 / 0.715 / 0.848。データ取得を変えたら、これが論文から離れていないか確認する。
- **CPI式（jp_inflation_v1）も前身記事と照合済み**。前身記事 4.2.1：原油(t−6) +0.802 / 為替 +0.742 / コールレート −0.195 / DW 0.399。こちら：+0.763 / +0.843 / −0.170 / DW 0.394。**DW がほぼ一致**しており再現できている。
  - CPI式の政策金利の係数は、標準偏差 0.157 で割ると **1pt の利上げで CPI −1.08pt** に相当する（前身記事の −0.195 だと −1.24pt）。推定期間の政策金利は最小 −0.071／最大 0.521／標準偏差 0.157 とほぼゼロ近傍しか動いておらず、シナリオで 1.00pt 動かすのは標準偏差の 6.4 倍、1.50% は推定期間の最大値の約3倍にあたる。**利上げシナリオの CPI の効きは、推定域外への外挿**である点に注意。両方の出典ともこの点には触れていない（前身記事は DW の低さから「有意性が過大評価されている可能性」には言及している）。
- **景気ウォッチャーは現状判断DI（方向性）の季節調整値（e-Stat 0003348423）を全式で使う**。水準DIより表4に近い。消費の式は論文で「現状判断水準」と書かれているが、状態変数は1つしかなく、同じ表現の6.3節（GDPギャップ式）は方向性DIで一致するため、統一した。
- 表4の3つ目の説明変数「GDPギャップ(t−1)」は**米国の**GDPギャップ（日本のにすると輸出の係数が 0.31 と大きく外れる）。GDPギャップは内閣府のもの（日銀の需給ギャップだと合わない）。
- 付録の表7（米国ブロック）は PDF 上で右端が切れている。各セル1行目の末尾数文字だけなので、構成は復元済み。
- 論文の中で食い違っている箇所は、付録（表7・表8）を優先した。
  - 表6のラグ（短観 t−6、在庫率 t−3、米賃金の景況感）
  - 7.2節の長期金利式の gdp_per_worker
- ラグの記載が無い説明変数は当期にした。定義が書かれていないもの（日米金利差の向き、実質政策金利＝政策金利−CPI前年比、log(CPI+3) の CPI は前年比、risk/inflation premium、gdp_per_worker）は独自に定義した（README に記載）。
- us_wage_v1 の「FF金利(当期)」は、FF金利が③で後から決まるので t−1。
- 論文の「実質消費支出(兆円)」は、単位から判断して QE の実質民間最終消費支出。出典に家計調査も挙がっているので、確定ではない。

## データ取得の癖

- **e-Stat の DB（getStatsData）は、表によって更新が止まっている**（毎月勤労統計は2014年、建築着工は2024年で止まっていた）。最新まで欲しいときは、公表 Excel を `estat.latest_catalog_file()`（getDataCatalog）や一覧ページから探す。statInfId やファイル名は公表のたびに変わるので、ID を固定しない。
  - 一般職業紹介状況（statsCode 00450222）は getStatsData に非対応。
- e-Stat は連続アクセスで 403 を返すことがある。`estat._get` で2秒間隔、403 のときは20秒待ってリトライ。
- 実質賃金は、確報を e-Stat 長期時系列表 表番号25-1（`.xls`、sheet TL）から、最新の速報を厚労省の月次公表 Excel から取り、重なる月は速報を優先する。指数から計算した前年比は公表値と最大3pt以上ずれるので使わない。
- 住宅着工の `.xls` は旧形式なので xlrd で読む。期間ラベルは和暦で、年は1月の行にしか書かれていない。
- 内閣府 QE の CSV と GDPギャップ Excel は、URL が公表ごとに変わるので、メニューページから最新を探す。
- 財務省の国債金利は、日次（jgbcm_all.csv + 当月 jgbcm.csv）を月平均にする。途中までしかない当月は除外する。
- BLS API v2 はキー無しだと1日25リクエスト・1回10年分まで。実質平均時給（CES0500000013）は FRED に無いので BLS から直接取る。
- **公式統計そのものの欠測**：米国の2025年10月（政府機関閉鎖で CPI・失業率・実質平均時給が未公表）、日本の就業者数の2011年3〜8月（震災）。推定ではその月を除き、シミュレーション開始時の履歴は前の月の値で補う。
- 公表時期が系列によって違う。2026-09-17 時点で全系列がそろう最新月は 2026-06（シナリオの `base_date` に使う）。

## 既知の性質・課題

- 前年差で推定する式と、水準の説明変数の組み合わせは、誤差が年ごとに積み上がって長期では発散しやすい（米失業率⇔米GDPのループなど）。合成データでは実際に発散したことがある。
- AR項の無い水準式（米個人消費など）は、予測1期目に実績から当てはめ値へ段差のように跳ぶ → アドファクターで緩和できる。
- 当てはまりが弱い式：jp_housing_starts（R² 0.17）、jp_gdp（0.33）、jp_economy_watcher（0.34）。
- 予測の信頼区間は出していない（論文 v1 と同じ）。前身記事も技術論文も同様で、技術論文の第10章は「不確実性（信頼区間）は現状では定量化されていない」と明記している。
- **コロナ期間の扱いが2つの出典で食い違う（未検証）**。技術論文の第10章は、構造変化を経た期間を「含む」ことを限界として挙げている。一方、前身記事の 2.2 節は「各方程式のサンプル期間にコロナ期間の部分除外を適用」と書いている。**このリポジトリは技術論文に従って除外していない**（`estimate.py` は欠測行を落とすだけ。N=217 = 2007-01〜2025-01 の全月）。前身記事とR²が乖離する式（jp_job_openings 0.660→0.396、jp_tankan_all 0.915→0.872、jp_consumer_sent 0.647→0.552、逆に jp_consumption 0.131→0.459）があり、これが一因の可能性がある。試すなら、コロナ期間を除いて再推定し、前身記事の値に寄るか確認する。
